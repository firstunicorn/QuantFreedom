Trading bot:
    ☐ Happy birthday!
    ☐ Add TimeScale DB
    ☐ Add trades history
    ☐ Add ability to look/show current pending orders
    ☐ Add feature to cancel pending orders
    
    ☐ Add ability to show current balance[and maybe record it to trade history?]
    

Trading bot logic itselt:
    ☐ Double check before orders submitting
    ☐ Instead of using local "switcher" variable "open_position = False/True" - fetch real data from exchange API

Frontend/Dashboard/Bot Control Panel with Flask:
    ☐ Make basic dashboard for bot control[status, balance, current orders, etc]
    ☐ Add ability to start/stop bot
    ☐ Add ability to change bot settings
    ☐ Add ability to change bot logic

Security:
    ☐ Make somehow works with hashes, not API keys directly, so hired developers can work on the server side without knowing the API key.
    ☐ Configure API key to be used only from specific IP addresses.

Server security:
    ☐ Install Fail2Ban # Configure Fail2Ban with bash script # https://webdock.io/en/docs/how-guides/security-guides/how-configure-fail2ban-common-services # https://www.linode.com/docs/guides/how-to-use-fail2ban-for-ssh-brute-force-protection/ # Monitoring With Fail2Ban-Client # fail2ban vs # CroudSec seems to be a decent replacement for fail2ban
    ☐ Change SSH port

Coding Productivity:
    ☐ SSH++ by Tablet Labs



chmod 700 /path/to/virtual/environment/files/*
chmod 600 /path/to/virtual/environment/

sudo chmod 700 /home/ubuntu/bot-diver/*
sudo chmod 600 /home/ubuntu/bot-diver/

ls -l /path/to/virtual/environment/files/
ls -l /home/ubuntu/bot-diver/

sudo chown -R ubuntu:ubuntu /home/ubuntu/bot-diver
sudo chmod -R 700 /home/ubuntu/bot-diver

RIGHT CHMODS:
    If you want to elevate your python script to sudo, try this chmod 740 my_script.py I am not a linux user, so I may be using the wrong chmod number
    
    700 for log folders (not less, because it will not be able to write to the file) (tested, works)
    664 for log files (not less, because it will not be able to write to the file) (tested, works)
    500 for secret folder (tested, works) (not less obviously, because it will not be able to read the file)

    750 for secret files (tested, works... maybe can be 640 or 600 or less
    500 for VENV folders (maybe 700)
    600 for VENV files (maybe 700)
    750 for .py files (tested, works... maybe can be 640 or 600 or less, like 500)

    sudo find /home/ubuntu/bot-diver/ -type d -exec chmod 600 {} \;
    ubuntu@vps-bbaa3c1d:~$ chmod -R 700 /home/ubuntu/bot-diver/
    ubuntu@vps-bbaa3c1d:~$ chmod -R 700 /home/ubuntu/bot-diver/divervenv


OLD CHMODS:
    
    chmod -R 750 /home/ubuntu/bot-diver/        !!!!!!!!!!  UPDATE: 664 IS THE BEST FOR LOG FILES
    chmod 500 filename.py
    chmod 550 filename.py - IF NOT WORKING
    chmod 700 /path/to/virtual/environment/files/*
    chmod 600 /path/to/virtual/environment/
    chmod 600 /path/to/logfile.log
    chmod 700 /path/to/log/folder/
    chmod 750 /path/to/virtual/environment
    chmod 640 /path/to/virtual/environment/bin/activate




    chmod -R 750 /home/ubuntu/bot-diver
    chmod -R 750 /home/ubuntu/bot-diver/divervenv
    chmod -R 600 /home/ubuntu/bot-diver/divervenv
    chmod 700 /home/ubuntu/bot-diver/divervenv/*



    chmod 640 /home/ubuntu/bot-diver/divervenv/bin/activate


*/2 * * * * /home/ubuntu/bot-diver/divervenv/bin/python3 /home/ubuntu/bot-diver/divervenv/binance-no-sockets.py >> /home/ubuntu/bot-diver/divervenv/logs/prints-outputs.log 2>&1
